describe("FireSprite.js", function () {
    var sprite = null;


    beforeEach(function () {
        sprite = spriteFactory.createFire();

    });
    afterEach(function () {
        sprite = null;
    });

    describe("构造函数", function () {
        beforeEach(function () {
        });

        it("调用父类构造函数", function () {
        });
    });


});